{
  "name": "jest-mem-leak",
  "version": "1.0.0",
  "description": "Testing jest@25 memory leak detection",
  "main": "index.js",
  "scripts": {
    "test": "jest --env=jsdom --detect-leaks --runInBand",
    "test:sixteen": "jest --env=jest-environment-jsdom-sixteen --detect-leaks --runInBand",
    "test:node": "jest --env=node --detectLeaks --runInBand"
  },
  "keywords": [
    "jest"
  ],
  "author": "Dylan Wulf",
  "license": "ISC",
  "dependencies": {
    "jest": "25.1.0",
    "jest-environment-jsdom-sixteen": "1.0.0",
    "weak-napi": "1.0.3"
  }
}
